const heartElements: NodeListOf<HTMLImageElement> =
  document.querySelectorAll("img.heart");

const heartOutlineElements: NodeListOf<HTMLImageElement> =
  document.querySelectorAll("img.heart-outline");

export { heartElements, heartOutlineElements };
